## 1. Model Change

- [x] 1.1 In `agent/models.py`, change `ProcessingOptions.ocr` default from `True` to `False`

## 2. Documentation

- [x] 2.1 In `README.md`, update the description of `options.ocr` to reflect `false` as the new default and note it as a breaking change
